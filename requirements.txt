# 鞋子相似度检测系统 - 依赖包
# Shoe Similarity Detection System - Requirements

# 深度学习框架
torch>=2.0.0
torchvision>=0.15.0

# AI模型
transformers>=4.30.0

# 图像处理
pillow>=10.0.0
rembg[cpu]>=2.0.50

# 数据处理
numpy>=1.24.0,<2.4
scikit-learn>=1.3.0

# 工具库
tqdm>=4.65.0

# Web界面
streamlit>=1.28.0

# 注意：
# 1. rembg[cpu] 用于CPU背景移除，如果有NVIDIA GPU可以改为 rembg[gpu]
#
# 2. 如果有NVIDIA GPU，建议安装GPU版本的PyTorch：
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# 3. 首次运行会自动下载AI模型（约500MB）
#
# 4. 推荐使用Python 3.10或更高版本
